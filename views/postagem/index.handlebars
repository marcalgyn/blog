

<div class="card">
    <div class="card-body">
        <h3>{{postagem.titulo}}</h3>
        <hr>
        <small>Data de Publicação: {{postagem.data}}</small><br>
        <hr>
        {{postagem.conteudo}}
    </div>
</div>